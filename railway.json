{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "node /serve-web.mjs",
    "healthcheckPath": "/hospitals",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE"
  }
}
